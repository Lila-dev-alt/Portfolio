{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}

{% block body %}
    <h1 class="text-center mt-3">Projet {{ project.name }}</h1>
<div class="container d-flex justify-content-center w-50">
    <table class="table table-hover mt-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ project.image }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ project.link }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <div class="bouttons d-flex justify-content-between mt-5 m-auto w-50 p-3">

    <a class="text-dark btn bg-warning" href="{{ path('project_index') }}">Retour à la liste</a>

    <a class="text-white btn btn-primary" href="{{ path('project_edit', {'id': project.id}) }}">Editer</a>

    {{ include('project/_delete_form.html.twig') }}

</div>
{% endblock %}

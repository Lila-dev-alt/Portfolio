{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1 class="text-center mt-3">Contact</h1>
<div class="container d-flex justify-content-center w-50">
    <table class="table table-hover mt-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contact.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>
</div>
<div class="bouttons d-flex justify-content-between mt-5 m-auto w-50 p-3">
    <a class="btn-dark" href="{{ path('contact_index') }}">Retour à la liste</a>

    <a class="btn-social" href="{{ path('contact_edit', {'id': contact.id}) }}">Editer</a>

    {{ include('contact/_delete_form.html.twig') }}
</div>
{% endblock %}

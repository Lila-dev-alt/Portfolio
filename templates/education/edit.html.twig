{% extends 'base.html.twig' %}

{% block title %}Edit Education{% endblock %}

{% block body %}
    <h1 class="bg-warning text-center mt-3">Editer une formation</h1>
<div class="container">
    {{ include('education/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    <div class="container d-flex justify-content-center">
    <a  class="btn bg-primary mr-2" href="{{ path('education_index') }}">Retour à la liste</a>

    {{ include('education/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
